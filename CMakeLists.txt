cmake_minimum_required(VERSION 3.15)
project(NWW2026)

# 添加 grader 子模块
add_subdirectory(grader)

# 添加 text_analyzer 子模块
add_subdirectory(text_analyzer)

# 如有其他 C/C++ 子模块，可继续添加
# add_subdirectory(other_module)
